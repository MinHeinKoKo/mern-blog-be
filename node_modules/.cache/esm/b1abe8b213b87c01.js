let fs,path;_13b‍.x([["fileRemover",()=>fileRemover]]);_13b‍.w("fs",[["default",["fs"],function(v){fs=v}]]);_13b‍.w("path",[["default",["path"],function(v){path=v}]]);


const fileRemover = (filename) => {
  fs.unlink(path.join(__dirname, "../uploads", filename), function (err) {
    if (err && err.code == "ENOENT") {
      //file doesn't exist
      console.log(`File ${filename} doesn't exit , won't remove it.`);
    } else if (err) {
      console.log(`Error occurred when trying to remove file ${filename}`);
    } else {
      console.log(`removed ${filename}`);
    }
  });
};

